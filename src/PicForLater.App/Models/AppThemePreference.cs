namespace PicForLater.App.Models;

/// <summary>
/// User-facing theme choices persisted independently of WinUI types.
/// </summary>
public enum AppThemePreference
{
    System = 0,
    Light = 1,
    Dark = 2,
}
